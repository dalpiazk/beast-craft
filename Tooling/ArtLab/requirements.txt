# Beast Craft art lab: the versions the starter trio was made with (Python 3.12, uv venv, Intel Arc XPU).
# torch/torchvision are the XPU builds; install them first from the PyTorch XPU index (see README.md, "Setup"):
#   uv pip install --index-url https://download.pytorch.org/whl/xpu torch==2.14.0+xpu torchvision==0.29.0+xpu
# then:  uv pip install -r requirements.txt
# On a CUDA or CPU machine use the matching torch build instead; the rest is the same.
diffusers==0.40.0
transformers==5.17.0
accelerate==1.15.0
safetensors==0.8.0
huggingface_hub==1.33.0
tokenizers==0.23.2
opencv-python-headless==5.0.0.93
numpy==2.5.3
pillow==12.3.0
